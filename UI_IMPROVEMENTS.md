- **App.tsx `getBranchColorMap()` uses hard-coded hex colors** that drift from the canonical branch tokens (`--branch-cork: #22c55e`, `--branch-mahon: #3b82f6`, `--branch-ms: #e8bf66`) already defined in `index.css` and used in `business.ts`/`OverviewTab.tsx`. Example: M&S is `#eab308` in `App.tsx` but `#e8bf66` elsewhere. Suggested fix: replace the inline map with a lookup into `business.ts` branch metadata or CSS variables so all branch colors resolve from one source.

- **AnalyticsTab.tsx hard-codes branch palette and mixes Tailwind palette classes.** `branchPalette` (lines 60-66) duplicates branch hex values and fallback colors instead of deriving from `BRANCH_META`/`business.ts` or CSS variables, and the tab uses arbitrary Tailwind palette classes like `bg-emerald-500/15`, `text-amber-400`, `bg-slate-900/60`, `border-slate-800/80`. Suggested fix: consume `BRANCH_META` for `main`/`dim`/`glow` values and replace palette classes with semantic token classes (`bg-[var(--accent-soft)]`, `text-[var(--ok)]`, etc.) so dark/light themes stay consistent.

- **FinanceTab.tsx and ReportsTab.tsx hard-code `#6366f1` indigo in chart palettes.** `FinanceTab.tsx` line 67 (`COLORS`) and `ReportsTab.tsx` line 12 (`BRANCH_COLORS`) add an extra raw hex color that is not defined in `index.css` and bypasses the dark/light theme system. Suggested fix: replace the literal `#6366f1` with `var(--violet)` (already declared in `index.css`) so both charts use a single source of truth for the accent/violet color.


- **App.tsx system-health badges use invalid/non-existent Tailwind classes** (`border-emerald-550/20`, `border-rose-550/20`, `border-amber-550/20`, and unused `status-healthy`/`status-critical`/`status-warning` text classes). Tailwind has no `*-550` shade, so the health-status border color was silently missing. Suggested fix: use the existing `.pill.ok`/`.pill.warn`/`.pill.bad` semantic tokens from `index.css` for both background and border/text color. Auto-fixed in this run.

- **Status badges in KpiCard, ActionQueue, and TargetsProductionTab mix Tailwind palette colors with semantic tokens.** `KpiCard.tsx` uses `bg-emerald-500/10 ring-1 ring-emerald-500/20` with `text-[var(--ok)]`; `ActionQueue.tsx` uses `bg-amber-500/10 text-[var(--warn)]`; `TargetsProductionTab.tsx` uses `bg-emerald-500/10 ring-1 ring-emerald-500/20`, `bg-amber-500/10`, and `bg-rose-500/10`. These hard-code Tailwind's emerald/amber/rose palette so the surfaces no longer track the CSS variables (`--ok`, `--warn`, `--bad`) defined in `index.css`. Suggested fix: derive the background/border colors from the same semantic variables (e.g. `bg-[var(--ok)]/10 ring-1 ring-[var(--ok)]/20`) or unify all status badges on the existing `.pill.ok`/`.pill.warn`/`.pill.bad` classes.

- **RagAnalyzerTab.tsx uses raw Tailwind slate/amber/red/emerald/purple utility classes and non-semantic surface tokens.** Multiple elements use `bg-slate-900/60`, `border-slate-800/80`, `text-slate-400`, `bg-emerald-500`, `text-red-400`, `focus:border-amber-500/60`, and `accent-purple-500`, bypassing the project's CSS variables and the `.card`/`.pill` design system. Suggested fix: refactor surfaces to use shared tokens — wrap metric cards with `.card`, replace palette colors with `var(--ok)`/`var(--warn)`/`var(--bad)`/`var(--violet)`, use `bg-[var(--panel)]`/`bg-[var(--surface)]` for backgrounds, and use `text-[var(--text)]` instead of `text-white` for primary copy. Consider adding `--accent-warn`/`--accent-bad` CSS tokens if alert tones need branch-aware theming.

- **RagAnalyzerTab.tsx uses inconsistent border radius (rounded-2xl) compared to the rest of the app (rounded-xl/.card).** Its cards use `rounded-2xl` while the shared `.card` class and most tabs use `rounded-xl` / `rounded-2xl` inconsistently; the largest drift is that this tab's search input and metric cards don't reuse `.card` or `.input` tokens. Suggested fix: wrap RagAnalyzer metric/section cards in the shared `.card` class (which gives `rounded-16px`/`rounded-2xl` consistently) and apply `.input` to the search box so spacing, radius, focus ring, and border track the design system.

- **OverviewTab.tsx issue badge mixes Tailwind rose palette with semantic status text.** Line 198 uses `bg-rose-500/10` / `border-rose-500/20` while the text is `text-[var(--bad)]`, so the badge surface won't track `--bad` theme changes and duplicates palette drift already noted in other status badges. Suggested fix: replace the inline classes with the shared `.pill.bad` class (or `bg-[var(--bad)]/10 border border-[var(--bad)]/20`) so the background, border, and text all resolve from the same semantic token.
